
# Prefer setting OPENAI_API_KEY in your environment or in a local .env file.
openai_api_key: ''

number_of_similar_images: 4

# this is for ask be feature i.e text generative AI (aka ChatGPT)
custom_params:
  model: 'gpt-4o-mini'
